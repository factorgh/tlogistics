/* eslint-disable react/prop-types */
const CustomHeader = ({ headerTitle }) => {
  return (
    <h1 className="text-3xl font-medium text-[#5F616E] mb-5">{headerTitle}</h1>
  );
};

export default CustomHeader;
